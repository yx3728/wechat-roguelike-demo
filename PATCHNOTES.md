# 数值改动记录 / Patch Notes

**维护约定**

- 每次调整战斗数值，都在本文件**最上方**追加一节，标题为 `## YYYY-MM-DD — 一句话主题`。
- 每条改动写成 `词条名（id）：改前 → 改后`，后面跟一句**为什么**。
- 涉及强度的改动，附一份满构筑实测对照（脚本见本文件末尾「怎么复测」）。
- 涉及机制（不只是数字）的改动，单独放在「机制改动」小节，并写清受影响文件与行为差异。
- 新增内容或改动菜单交互 / 存档结构时同样记一节，写清是否需要旧存档迁移。

**数值所在文件**

| 文件 | 内容 |
| --- | --- |
| [src/upgrades.js](src/upgrades.js) | 局内进化（三选一词条）全部数值与前置链 |
| [src/battle.js](src/battle.js) | 战斗结算：伤害/护盾/反伤/回复、敌弹与撞机伤害、无尽成长曲线 |
| [src/enemies.js](src/enemies.js) | 敌机 / 精英 / Boss 血量与速度成长 |
| [src/talents.js](src/talents.js) | 局外天赋（永久加成）与花费 |
| [src/characters.js](src/characters.js) | 角色基础属性与解锁价格 |
| [src/maps.js](src/maps.js) | 地图（背景色带 / 颗粒风格 / 刷怪池 / 沙暴配置 / 解锁条件） |
| [src/storage.js](src/storage.js) | 存档结构与解锁写入 |
| [subpackages/pkg_boss/src/bossAi.js](subpackages/pkg_boss/src/bossAi.js) | Boss 招式、连招与 AI 驱动 |

---

## 2026-09-07 (14) — 新地图「地狱」：业火回魂

新增第五张地图，通关草原后解锁。完整设计与验证过程见 [docs/hell-map.md](docs/hell-map.md)。
本节只记数值与机制；这是新增内容，不影响旧存档（`unlockedMaps.hell` 默认 false，草原通关时置位）。

### 机制改动：业火回魂（`hellfireRevenant`）

前四张图里都是**地图**在给玩家施压（沙暴/潮汐是定时器，草原是静态地形）。
地狱换了压力来源：**玩家自己的击杀速率**。

- 杀死普通敌人 → 原地掉「魂火」，**4.5 秒**引信（焦骨掉的只有 **3.0 秒**）
- 玩家中心 **28 px** 内碰到 → 「镇魂」：+1 层业火（**+7% 主炮伤害**，每层独立 **4.0 秒**计时，上限 **6 层**）
- 引信烧完 → 「回魂」：原地站起一只亡魂（源敌人 **45% 生命**、移速 **1.8×**、只有接触伤害）

**两条红线，改数值时不要碰**（各有一条专门的测试用例）：

1. **击杀亡魂不产生新魂火** —— 否则回路递归，场面永远收不回来
2. **场上魂火 ≤ 14、亡魂 ≤ 8** —— 无论玩家 DPS 多离谱，场面有天花板；
   到达亡魂上限时烧完的魂火直接消散，不再惩罚已经被淹的玩家

受影响文件：`src/hellMechanics.js`（机制本体，不认识敌人类型，回魂只往队列排请求）、
`src/battle.js`（`applyHellfireResults`：兑现亡魂、结算镇魂爆、施加锁链拖拽；
主炮伤害乘业火层；「无罪」在扣血前挡一次）。

### 新增敌人与 Boss

| 名称 | id | 预警 | 关键数值 |
| --- | --- | --- | --- |
| 焦骨 | `cinderHusk` | 1200 ms | 生命 2 档，掉的引信 **3.0 秒** |
| 拾魂者 | `soulPicker` | 不攻击 | 抢魂火；每吃一个 **+30% 生命**并变大，自己的引信 **8.0 秒** |
| 烙印使 | `brandBearer` | 900 ms | 印记半径 **46 px**、延迟 **1.1 秒**；**盖到魂火上直接点燃** |
| 锁魂 | `chainWarden` | 1000 ms | 拖拽 **118 px/s**、持续 **2.2 秒**，方向朝最近的魂火 |
| 炉喉 | `forgeGullet` | 1300 ms | 3 颗慢速炉火，飞 **3.0 秒**后停在原地再烧 **2.6 秒** |
| 刑官（精英） | `warden` | 850 ms | 光环半径 **132 px** 内引信 **2× 速**；死亡时全场引信压到**剩余 1.5 秒** |
| 亡魂 | `revenant` | 起身 420 ms | 源敌人 **45% 生命**、**1.8× 速**、只有接触伤害、不掉落任何东西 |

**阎罗（`yama`）**：血量沿用统一定标 `(15 + lv/2) × 100000`，**没有额外堆血**。
难度由「罪值」承担：0–100，开局 20，**回魂 +15 / 镇魂 −6**（二阶段镇魂只减 3.6）。

- 罪值 **< 32**：只用「锁刑」，且阎罗**承伤 ×1.30**
- 罪值 **≥ 32**：解锁「业火判决」（按距玩家由近及远依次点燃场上魂火，间隔 350 ms）
- 罪值 **= 100**：立刻打出「无间」，之后重置为 50

这条设计的用处：**Boss 可以做得很难而不必堆血量**——之前反复调 Boss 血量时缺的就是这根杠杆。
基础金币 **1400**，不连接隐藏虚空线。

### 锁刑的走廊（实测，不是公式推的）

| | 一阶段 | 二阶段 |
| --- | --- | --- |
| 墙数 / 间隔 | 4 道 / 620 ms | 6 道 / 480 ms |
| 缺口摆幅 | ±110 px | ±140 px |
| 缺口宽度（每行一致） | **63 px**（玩家宽度 1.97 倍） | **63 px** |
| 缺口横移需时 / 间隔 | 299 ms / 620 ms | 390 ms / 480 ms |
| **余量** | **299 ms** | **61 ms** |

63 px 落在已发货 Boss 走廊 **45–89 px** 的紧端；二阶段 61 ms 是全作最紧的一处。
无间预警 **1500 ms**，硬约束「两个安全格中至少一个与玩家当前格相邻」保证最坏位移只有 **248 px**
（只需 165 px/s）。以上全部由 `tests/hell.test.js` 长期锁住。

### 六条地狱专属海克斯

| 品质 | 名称（id） | 效果 |
| --- | --- | --- |
| 蓝 | 引魂（`hell_draw`） | 镇魂判定半径 **28 → 98 px** |
| 蓝 | 缓刑（`hell_reprieve`） | 所有魂火引信 **+2.0 秒**（现存的也生效） |
| 绿 | 余烬不熄（`hell_emberhold`） | 业火上限 **6 → 9**，每层计时 **4.0 → 5.5 秒** |
| 紫 | 镇魂爆（`hell_quell`） | 每次镇魂对 **90 px** 内敌人造成主炮伤害 **×2.5** |
| 橙 | 无罪（`hell_absolution`） | 满层时免疫一次伤害并清空全部层数，冷却 **12 秒** |
| 蓝 | 赦令（`hell_pardon`） | 击杀亡魂返还 **1 层业火** |

掉落：普通敌人经验球 **50%** 概率、道具率 **×0.25**（全作最紧，比沙漠的 0.5 / 0.3 还紧）。

### 落地时改了什么

「这一章难度高一些」的整体收紧：引信 6.0 → **4.5 秒**、亡魂 35%/1.6× → **45%/1.8×**、
上限 10/6 → **14/8**、业火 +8%每层5秒 → **+7%每层4秒**、罪值 +12/−8阈值40 → **+15/−6阈值32**、
低罪承伤 ×1.35 → **×1.30**、无间预警 1800 → **1500 ms**。

**锁刑重做了一次。** 原方案是"整圈旋转环"，公式算出来 87 px 走廊看着没问题，
但把真实发射的子弹量一遍发现两个错：抠 k 发留下的是 **k+1 格**缺口；
更要命的是竖屏几何本身不成立——整圈弹幕的缺口转到侧面时，玩家那一排就是一堵没缺口的墙，
实测二阶段连续两环缺口错开 136°，需要横移 1770 ms 而只有 448 ms，**无解**。
改成**朝下的平行弹幕墙**（缺口横坐标是唯一变量、每行等宽、左右来回摆而非单向转走）后
才有了上表那组数字。教训：弹幕的可解性要从**真实发射出来的子弹**上量，不能信公式。

### 测试

`tests/hell.test.js` 十条；`tests/texture-coverage.test.js` 新增 procedural 分支——
地狱敌人是画出来的不是贴图，必须被 `hellVisuals` 认领且一张图片都不加载。
全套 `node --test tests/*.test.js` 共 83 条通过。

## 2026-09-02 (13) — DEV 控制台重写：声明式 schema + 布局引擎

面板是随功能一点点长出来的，攒下四个结构性问题：

1. **布局全是互相牵制的魔法数字**。`devRowYs()` 里 `rowStep = 58`、`forceStatusReserve = 52`、`coinsGapBelowForceStatus = 14`、`hintY = coinsRow + 36`，外加词条卡片区的条件锚定——加一行要重算一串。
2. **绘制和点击各写一遍坐标**。`drawDevPanel`（约 270 行）与 `handleDevTouch`（约 165 行）各自调 `devRowRect` 推算位置，改一处忘一处就会点不中。
3. **没有分组、不能滚动**，所有内容必须塞进一屏——这正是布局要那么拧巴的原因。
4. **新内容一个都没进来**：地图、旱魃、沙暴全是后加的，控制台里根本调不到。另外 `applyDebugStart` 只认 crimson / azure / void，**就算加了旱魃选项也是无效的**。

### 一、声明式 schema

全部内容集中在 `DEV_SCHEMA` 一个数组里，四种行类型：

| 类型 | 用途 |
| --- | --- |
| `choice` | 单选互斥，如起始等级 / 地图 / Boss 形态 |
| `toggles` | 多个独立开关，如无敌 / 一击 / 十倍速 |
| `actions` | 点一下就执行，如 +金币 / 解锁全部 |
| `cards` | 词条自选网格（分页，只在需要时出现） |

加一行只要往数组里加一项，**布局、绘制、点击自动跟上**，不用碰任何坐标。

### 二、布局引擎：绘制与点击共用一份

`devLayout()` 走一遍 schema，算出一串带矩形的元素（`section` / `label` / `btn` / `card` / `cardsNav`）。`drawDevPanel` 和 `handleDevBodyTap` **读的是同一份结果**，从结构上杜绝了两边不一致。

按钮超过 `DEV_MAX_PER_LINE`（5）自动换行，所以 Boss 形态那种 6 个选项的行不用再手动拆。

### 三、面板体可滚动

标题栏 / 可滚动体 / 底栏三段式。体区裁剪 + 拖拽滚动 + 右侧滚动条，内容超出一屏时才画滚动条。拖拽超过阈值不算点击，避免滚动时误触按钮。

底栏（关闭 / 重置存档 / 以调试启动）固定不滚。

### 四、补齐缺失内容

| 新增 | 说明 |
| --- | --- |
| **地图** | 星空 / 沙漠切换，且**选中即解锁**——调试就是要能去没解锁的地方 |
| **Boss 形态：旱魃** | 同时修了 `applyDebugStart` 只认三种 variant 的 bug，改为 `FORCEABLE_BOSS` 白名单 |
| **沙暴（仅沙漠）** | 正常周期 / 强制常驻 / 强制关闭。`updateSandstorm` 顶部接管，跳过周期直接钉住 |
| **解锁行** | 一键解锁全部角色 / 全部地图 |
| **金币 +100 万** | 原来只有 +1000 / +10000 |
| **标题栏摘要** | 右上角实时显示当前作弊态（`无敌 · 秒杀 · 10x · 沙暴:on`），不用滚下去确认 |

### 五、两套词条自选流程合并

原来 `upgradeStartMode: "pickCards"`（起手词条）和 `FORCE_UPGRADE_ACTIONS`「菜单选取」（本局必出）共用一个 `devUpgradePickerTarget` 和一个分页索引，互相重置的逻辑绕来绕去。

现在统一成一个 `devPickerTarget`（`null` / `"start"` / `"force"`），网格标题直接写明**正在为谁选、已选几条**。

### 六、顺手清掉的

- 三段被注释掉的死代码（force 状态文案、说明行等）
- `DEV_PANEL_BODY_TOP`、`devRowYs`、`devRowRect`、`drawDevRow`、`getDevUpgradeNavRects`、`getDevUpgradeCardRect` 六个不再需要的函数
- 配色散落各处 → 集中到 `DEV_C`

### 验证

新增 `t_dev`，连同已有的共 **18 个测试台、各跑三遍**全过：

- 未解锁时主菜单不露出 DEV 入口；解锁后能打开。
- 五个分组、十行标题全在（滚动全览后统计——视口外的行不绘制，这是刻意的省绘制）。
- Boss 形态里有旱魃；地图选项两张都在。
- 点「沙漠」后 `selectedMapId` 真的变了，且顺手解锁。
- 拖拽后内容位置改变（滚动生效）；滚动过程中不会误触金币按钮。
- 「以调试启动」传出的 payload 十个字段齐全（含新增的 `sandstorm`）。
- battle 侧：`sandstorm=on` 开局 2 秒内就有沙幕，`sandstorm=off` 跑过 35 秒仍无，`startBossVariant=hanba` 不报错。


### 追加：内容注册表（针对「以后加地图会不会越堆越乱」）

上面只把**面板**做成了声明式，**内容仍是手写清单**——加一张图要同时改 menu.js 的 Boss 列表、battle.js 的白名单、enemies.js 的两处 if/else，漏一处不报错。这一段把数据源统一掉。

**改前，加一个 Boss 要改 4 处**（enemies 配色表 / battle 白名单 / menu 选项 / bossAi 分派），**加一张地图要改 6 处**。

#### 注册表

| 注册表 | 位置 | 内容 |
| --- | --- | --- |
| `BOSS_VARIANTS` | enemies.js | id / 名字 / 配色 / `forceable` / `leadsToHidden` / `continuous` / `coin` |
| `ENEMY_POOLS` | enemies.js | 刷怪池，每项 `{ type, weight, minWave }` |
| `ENEMY_FACTORIES` | enemies.js | 敌人类型 → 工厂 |
| `ELITE_FACTORIES` | enemies.js | 精英类型 → 工厂 |
| `MECHANICS` | **新增 mechanics.js** | 玩法机制，声明 `fields` / `update` / `devModes` / `applyDev` |
| `MAPS` | maps.js | 只用 id **组合**上面这些，不写任何玩法逻辑 |

#### 引擎去硬编码

battle.js 里所有按内容名分支的逻辑都拆掉了：

| 原来 | 现在 |
| --- | --- |
| `mapEnemyPool === "desert" ? createSandworm : createElite` | `createEliteFor(state.mapEliteType, …)` |
| `mapEnemyPool === "desert" ? "hanba" : (随机红蓝)` | `pickMapBossVariant(battleMap)` |
| `FORCEABLE_BOSS = ["crimson","azure","void","hanba"]` | `forceableBossIds()` |
| `storage.unlockMap("desert")` | `mapsUnlockedByHiddenBoss().forEach(...)` |
| `bossVariant === "crimson" \|\| === "azure"`（隐藏 Boss 触发） | `bossLeadsToHidden(id)` |
| `bossVariant === "azure" && azureMode`（停火判定） | `bossIsContinuous(id)` |
| `bossVariant === "azure" ? 1000 : 800`（掉金币） | `bossCoin(id)` |
| 72 行 `updateSandstorm` 专属驱动 | `updateMechanics(dt)` 走注册表 |

enemies.js 里 `pickEnemyType` 的 if/else 与 `spawnEnemyForWave` 的 switch 也换成了查表。

#### DEV 面板同步派生

- **Boss 选项**从 `BOSS_VARIANTS.filter(forceable)` 生成——加 Boss 自动出现，不会再有"菜单里能选但实际不生效"。
- **机制行**按当前地图启用的机制动态插入：星空没有沙暴，那一行连同「战场机制」分组标题一起不出现；切到沙漠才有。
- DEV payload 的 `sandstorm` 字段换成通用的 `mechanics: { 机制id: 档位 }`。

#### 校验器（防烂的关键）

`validateMaps(reg)` 检查每张地图引用的 `enemyPool` / `eliteType` / `bossVariant` / `mechanics` / `themes` 是否都已注册，**串错 id 当场报出来而不是静默走默认值**。新增测试台 `t_content` 调用它，并额外验证：

- 故意塞一张全串错的地图，能报出 5 类错误（校验器本身有效）
- 每个池里登记的敌人都刷得出来，且不会刷出没登记的
- 每个 Boss 的配色来自注册表、互不重复、在 bossAi 里都有分派
- 每个精英工厂造出来的都标了 `isElite`，未知类型回落到默认
- 每个机制都声明了 `fields` / `update`，有 `devModes` 就必须有 `applyDev`
- 专属词条没有指向不存在的地图
- **battle.js 里不再存在按地图名分支的代码**（正则扫源码）

#### 现在加内容的成本

| 加什么 | 要改的地方 |
| --- | --- |
| 新杂兵 | enemies.js：写工厂 + 登记进 `ENEMY_FACTORIES` + 加进某个池 |
| 新精英 | enemies.js：写工厂 + 登记进 `ELITE_FACTORIES`；地图 `eliteType` 指名 |
| 新 Boss | enemies.js 加进 `BOSS_VARIANTS`；bossAi.js 写 driver + 分派 |
| 新机制 | mechanics.js 加一项；地图 `mechanics` 里引用 |
| **新地图** | **maps.js 加一项把上面串起来 + storage.js 补默认解锁值** |

`battle.js` / `menu.js` 都不用动。串错了 `t_content` 会报。


### 踩到的坑

替换布局引擎那段时锚点选得太宽，**把中间的设置面板函数一起吞掉了**（`getSettingsButtonRect` 等 9 个 + `tryRedeemByCode`）。`node --check` 过得去——这些是运行时才解析的引用，语法检查看不出来，是测试第一条就崩了才发现。已从备份取回。教训：跨大段替换前先列一遍该范围内的函数名，确认没有无关的东西夹在中间。

---

## 2026-09-02 (12) — 沙漠第三批：八条地图专属海克斯

「沙漠刷不出专属海克斯」不是 bug——**这批内容一直没做**。前两批只做了沙暴、杂兵、精英、Boss，词条池 62 条全是通用的。本节补齐。

按设计稿里已定案的 Q3 执行：**八条全留，靠抽取权重补**（不砍条目），权重倍率初值 2.0。

### 一、地图门槛机制

`entry()` 的 `options` 加 `maps` 字段（数组，**不填 = 全地图通用**），`available()` 里比对 `state.mapId`。62 条通用词条一条没动。

### 二、抽取权重补偿

沙漠池 = 62 通用 + 8 专属。按同稀有度等权抽的话单条出率只有 1/70，一局下来大概率一条都见不到。新增 `MAP_EXCLUSIVE_WEIGHT_MUL = 2`，专属条目的稀有度权重乘这个系数，但仍受稀有度约束（橙的专属照样稀有）。

实测（20000 次三选一）：

| 倍率 | 含专属的面板占比 | 专属占全部卡位 |
| --- | --- | --- |
| 1.3 | 47.1% | 18.3% |
| 1.6 | 52.9% | 21.0% |
| **2.0（现行）** | **59.5%** | **24.7%** |

**这个出率偏高，需要你实机确认**：59.5% 意味着一局 12 次升级里约有 7 次会看到专属词条，而专属只有 8 条——一局沙漠基本能把它们凑齐，构筑差异会被拉平。定案写的是"初值 2.0，进游戏体感后再调"，所以我按 2.0 发了；觉得多的话把 `MAP_EXCLUSIVE_WEIGHT_MUL` 调到 1.3 即可（上表是现成对照）。

### 三、八条词条

全部围绕沙暴：要么对抗它，要么押注它。

| 稀有度 | 名字 | 效果 | 字段 |
| --- | --- | --- | --- |
| 绿 | **逆风者** | 沙暴期间射速 +40% | `stormFireRateMul` |
| 绿 | **拾荒者** | 沙暴期间拾取的金币 +200% | `stormCoinMul` |
| 蓝 | **沙镜** | 沙暴期间敌人不再随距离在风沙里淡化 | `stormClearSight` |
| 蓝 | **风蚀** | 命中使目标受到的伤害 +12%，可叠 3 层、持续 4 秒 | `erosionPerStack` / `erosionMaxStacks` |
| 紫 | **热寂** | 敌人移速每高 10%，对其伤害 +8%（沙暴期敌人加速 50% → 全体 +40%） | `heatDeathPerTenth` |
| 紫 | **流沙缚** | 拾取范围内敌人额外减速 30%，与时间流护盾叠乘 | `quicksandBind` |
| 橙 | **沙暴之眼** | 沙暴期间视野完全不受影响，且每秒回复 2% 最大生命（前置：沙镜） | `stormEyeRegenRatio` |
| 橙 | **旱魃之息** | 沙暴持续 +50%，且沙暴期间全部伤害 +60% | `stormDurationMul` / `stormDamageMul` |

### 四、两条按实际机制改写了效果

设计稿写于第一版实现之前，有两条对不上现在的代码，**按实际机制写而不是照抄**：

1. **沙镜 / 沙暴之眼**：设计稿写的是"可视半径 +80%"和"不受视野限制"——那是第一版"暗幕挖洞"的说法。第 (6) 节已经把视野受限改成了**敌人随距离淡化**，所以这两条改成直接取消淡化，否则会去改一个已经不存在的半径。
2. **拾荒者**：设计稿写"击杀金币 +200%"，但击杀早就不直接给金币了（金币只从 `coin` 道具来，见 `onEnemyDefeated` 的注释）。改成接在**拾取金币道具**上，效果等价。

### 五、战斗接入（[battle.js](src/battle.js)）

- **逆风者**：结算时 `shootInterval / stormFireRateMul`，只在沙暴期生效，不永久改 `shootInterval`；仍受 `shootIntervalFloor` 约束。
- **沙镜 / 沙暴之眼**：`sandstormEnemyAlpha()` 开头直接返回 1。
- **风蚀**：新增 `applyErosionStack()` / `erosionMul()`，层数与到期时间挂在敌人身上。**叠层在伤害结算之后**，所以第一发不吃自己的加成。
- **热寂 + 旱魃之息**：新增 `desertDamageMul()`，接在子弹伤害结算里（Boss 易伤倍率之后）。热寂读 `sandstormMoveMul` 反推敌人超速比例。
- **流沙缚**：新增 `applyQuicksandBindSlow()`，挂在沙暴位移倍率之后，拾取范围内本帧位移再 ×0.7。
- **沙暴之眼回血**：每满 1000ms 回复 `baseMaxHp × 2%`，走 `healPlayer` 所以吃「超量血库」那套转化。
- **旱魃之息**：沙暴 `activeMs × stormDurationMul`。

### 验证

新增 `t_hex`，连同已有的共 **17 个测试台、各跑三遍**全过：

- 八条都带 `maps: ["desert"]`；通用词条仍是 62 条。
- 星空 4000 次三选一里**一条专属都没出现**；沙漠 4000 次里刷出 7/8 种（沙暴之眼有前置，新状态下抽不到属正常）。
- 沙暴之眼在没拿沙镜前不可用，拿了之后解锁。
- 八条的字段写入值逐个核对。
- 两张地图各跑 112 秒 `update + draw`（顺手点掉升级面板让词条真的被拾取）无异常。

### 已知遗留

- **权重 2.0 的出率偏高**（见上表），等你实机体感后定。
- 「热寂」的增伤对**所有**敌人生效，不只沙暴期——沙暴期敌人加速 50% 只是让它同时吃满。平静期若有别的加速来源（目前没有）它也会吃到。设计如此，不是 bug。
- 专属词条目前只有沙漠有。星空仍是纯通用池——机制已经现成，以后想给星空来一组，加 `maps: ["starfield"]` 即可。

---

## 2026-09-02 (11) — Boss 血量重定标：正常玩家 5 分钟打完

> **本节撤销第 (2) 节的「Boss 血量与伤害保持不变」决定。**
> 那一条写的是"后续若还要削玩家，默认同样不要跟着削 Boss"——现在不成立了，别再照着执行。
> 保持高压的目标没变，但 1990 万血对应的是 **19~28 分钟**，那不是难度，是耗时。

### 问题

实测现行 Boss 血量下，正常成长的玩家要打 **1135~1700 秒**（19~28 分钟）。

之前几轮我一直用"满构筑"估算（对 Boss DPS 755 万），严重高估了实际水平。用真实模块建模后：

| 指标 | 实际值 |
| --- | --- |
| Boss 出场（第 90 秒）时的等级 | **Lv.8~9** |
| 此时手上的词条数 | **约 8 个** |
| 此时对 Boss DPS | **4,500 ~ 12,800**（角色差异很大） |

关键发现：**经验只来自拾取的经验球**，`enemy.exp` 字段其实没被用上。普通敌人 70% 绿球(1) / 30% 蓝球(3)，均值 1.6；精英 85% 紫球(8)。90 秒内总共约 400 点经验，对应 Lv.9。所以玩家远没有我之前假设的那么强。

### 改动

| 目标 | 改前 | 改后 | 模型 TTK |
| --- | --- | --- | --- |
| 主 Boss（红 / 蓝 / 虚空一阶段） | `(192 + lv/2) × 100000` | **`(15 + lv/2) × 100000`** | 280~309s |
| 虚空核心（二阶段） | `(496 + lv/2) × 100000` | **`(22 + lv×0.8) × 100000`** | 347s |

实际血量：

| 等级 | 主 Boss | 虚空核心 |
| --- | --- | --- |
| Lv.8 | 190 万 | 280 万 |
| Lv.9 | 190 万 | 290 万 |
| Lv.15 | 220 万 | 340 万 |
| Lv.20 | 250 万 | 380 万 |

虚空核心系数比主 Boss 高一档，因为打到那里时玩家已经先过了一个主 Boss、成长更高。

**想改快慢就动那两个常数（15 / 22），它们基本线性对应 TTK。**

### 定标方法（`scratchpad/ttk.js`，可复跑）

用真实模块推算，不拍脑袋：

- 刷怪节奏取自 `battle.js`：波1 每 750ms 上限 4；波2 每 1000ms 上限 5、55% 出精英；Boss 战每 1000ms、15% 精英 / 25% 普通。
- 经验球分布与 `dropLootFromEnemy` 一致，升级曲线用真实的 `calcNextExpToNext`。
- 词条用真实 `UPGRADE_POOL` 加权抽 3 选 1，选卡策略是"能选到提升 DPS 的就选，否则随机"（会玩但不查表）。
- Boss 战期间继续刷怪、继续升级、继续选卡——DPS 是动态涨的，不是固定值。

**明确写出来的假设**（不同意就改这两个数再跑）：

- `HIT_RATE = 0.55`：散射打 Boss 的估计命中率。
- 玩家能清掉刷出来的怪、能捡到球。

四条成长路线（破袭者无天赋 / 破袭者中天赋 / 重炮手中天赋 / 侦察兵中天赋）各跑 24 局取均值。

### 已知遗留

- **角色之间的 TTK 差异很大**：同一血量下重炮手约 220s、侦察兵约 385s，接近 1.8 倍。这是角色本身的 DPS 差异，不是 Boss 血量的问题；要抹平得调 `characters.js` 的基础值，属于另一件事。
- 模型假设玩家能清怪能捡球。真实玩家走位失误、漏球会更慢；反过来构筑成型好会更快。**先按这个数值试玩，觉得快了慢了直接调那两个常数即可。**
- 无尽模式的血量倍率（`ENDLESS_HP_EXP_BASE = 1.062`，按等级指数放大）没动，仍然叠在新基数上。

---

## 2026-09-02 (10) — 弹幕分层：Boss 出招时杂兵停火 + 全 Boss 可解性验证

**问题**：旱魃 Boss 战里精英也在开火，两层弹幕叠起来经常无处可躲。

上一节的可躲性分析**只单独测了每一招**，漏掉了实战条件——Boss 战期间每 1.2 秒还在刷怪（上限 4 只，15% 概率出精英）。沙漠里就是沙蠕的 5 路扇形 + 响尾炮的弹墙 + 旱魃的弹幕三层叠加，Boss 招式里精心留出的走廊被直接填掉。

### 一、弹幕分层（不是删弹，是串行化）

新增 `updateAddFireSuppression()`（[battle.js](src/battle.js)）+ `maybeFire` 顶部拦截（[enemies.js](src/enemies.js)）：

- **Boss 出招期间，同屏杂兵与精英停火**。它们照样移动、照样撞人、照样得清——压力还在，只是不再往画面里塞第二层弹幕。
- **过热破绽窗口里杂兵恢复开火**。这样"趁破绽输出"本身带上代价，破绽不再是白送的。
- 停火期间冷却继续走，所以恢复时不会攒出一次齐射。
- 红/旱魃看 `currentAttack`，蓝看 `azureMode`，虚空线全程压制（它本来就是连续输出型）。

### 二、全 Boss 可解性验证（新增 `t_solvable`）

真实驱动 `bossAi.updateBoss`，按战斗里的真实规则挂上同屏杂兵与精英（走真正的 `maybeFire` / `updateEnemy`），每 96ms 采一次「玩家带里最宽的连续安全走廊」，对整场取最小值。血量从满线性掉到残血，覆盖全部阶段。

| Boss | 单挑 | 叠加杂兵 |
| --- | --- | --- |
| 红（crimson） | 49px | 45px |
| 蓝（azure） | 77px | 89px |
| 旱魃（hanba，含沙暴） | 53px | 53px |
| 虚空（void） | 28px | 28px |

机体宽 24px，且 `CLEARANCE` 已含机体半宽——所以"49px 走廊"指的是**机体中心**还能自由移动 49px 的安全带。全部通过，**6 组随机种子复验都通过**。

### 三、验证过程中揪出的三个真 bug

1. **连招会把前后两招的弹幕叠在一起**（单招分析看不出来）。`omniBurst` 在实战里只剩 8px 走廊。根因是它的 `move` 用了大幅漫游（振幅 1 / 1.5）——而走廊是以 Boss 当前位置为原点算的，先后发射的弹各自朝不同原点扩散，世界坐标系里那条走廊被自己的位移抹平了。改为出招时**站定**（振幅 0.18 / 0.12），走廊恢复到 45px 以上。

2. **裂地矛的缺口宽度没随矛数一起加宽**：P3 有 11 根矛，单格只有 W/10≈39px，扣掉判定后只剩 11px——和 `walledBarrier` 当初一模一样的坑。改为缺口宽度随矛数缩放（N≥11 用 3 格），并统一走 `inGapSector`，预警也带上真实宽度。

3. **虚空 `hatredBurst` 的仇恨点常落在相邻格**，4 组扇形几乎重叠：弹量翻倍但威胁不变，只是把走廊填死。按发射角去重（夹角 < 19° 视为同一束）。

### 四、指标本身的教训

第一版用「玩家带被弹幕覆盖的百分比」，**对整面墙类招式根本不成立**——墙就该覆盖全屏、只留缝，覆盖率必然很高。换成「最窄时刻的最宽连续安全走廊」才真正对应"能不能无伤过"。

另外追踪弹（虚空的 `fanPredict`、`hatredBurst`）在这个指标下天然吃亏：它们本来就该覆盖你现在站的地方，靠移动躲而不是靠找静态缝。所以虚空的数字偏低是指标偏差与设计意图共同作用的结果，不完全是难度问题。

### 已知遗留

- **虚空线余量最小（28px）**，是四个 Boss 里最紧的。它是隐藏 Boss、最难内容，且大量招式是追踪型，我认为这个余量可以接受；但如果实战里觉得残血期还是太堵，最直接的调法是把 `updateBossVoid` 里的 `enrage`（`1 + (1 - hp/maxHp)`，残血时接近 ×2 射速）封顶到 1.6。
- 停火规则对**所有**地图生效，不只沙漠。星空的红/蓝 Boss 战同样不再有杂兵弹幕叠加——这是一致性改动，但确实也让星空 Boss 战比以前好躲了一些。若想保留星空的原有压力，可以把压制做成地图开关。

---

## 2026-09-02 (9) — 红蓝 Boss：弹幕可读性 + AI 升级

两件事：**弹幕密到没有可穿的缝**（无伤过关只剩"靠回血硬吃"一条路），以及红蓝 Boss 的 AI 和改前的旱魃一样是老虎机。

**前提**：不降难度。实测下来弹量不但没减，`omniBurst` 反而从 684 涨到 798。

### 一、弹幕可读性：密可以，但必须有走廊

先在 `bossAi.js` 顶部写死了三条硬规矩（改招式前必读）：

1. 环形弹必须留缺口扇区，且缺口要**连续、缓慢移动**，不能每轮随机换位置。
2. 每轮旋转量要和弹间距成简单比例，否则后一环把前一环的缝正好填死。
3. **不要用 `Math.random` 抖基准角**——随机 = 不可读 = 只能靠运气；要抖就用 `elapsed` 的正弦。

新增共用工具 `inGapSector(i, N, gapStart, gapCount)`。

| 招式 | 问题 | 改法 | 弹量 |
| --- | --- | --- | --- |
| `ringSpin` | 每轮转 0.35 rad，而弹间距 0.449 rad，两者失配 → 后一环填死前一环的缝 | 每轮**正好转一个弹位**（2π/N），缺口连成稳定螺旋通道；缺口 2/14，每 3 轮挪一位；间隔 240→200ms | 140 → **144** |
| `omniBurst` | 18 发 × 110ms × 4.2s = 684 发，缺口跟着环转 | 缺口改为**世界坐标系**的固定走廊（朝下 ±30°，整招内缓慢摆 ±60°）；间隔 110→79ms | 684 → **798** |
| `walledBarrier` | 缺口 1 格（W/12≈32px），扣掉弹体与判定后可穿宽度**几乎为零**；且每轮随机瞬移 | 格数细分到 14、缺口占 3 格（≈84px）、**每两轮才漂一格**；间隔 380→300ms | 70 → **80** |
| 蓝 `rain` | 基准角用 `Math.random()` 抖动，缝隙开合全看运气 | 改为 `Math.sin(azureModeElapsed × 0.0042)`，覆盖范围一样但可预判 | 不变 |

`omniBurst` 那条尤其值得记：缺口**必须定义在世界坐标系**。这招同时有二三十代弹在飞，缺口若随环旋转，各代楔形指向早已差出几百度，彼此把对方的缝填死——实测只剩 8~12px 可穿。钉在世界角度后所有世代让开的是同一条走廊，实测 130px。

### 二、共用 AI 机制（三个 Boss 都用）

把旱魃那套抽出来，红/蓝/旱魃共用：

- **热度网格** `updateBossHeat` / `hottestColumn` / `coldColumn` / `hottestCell`（字段 `boss.bossHeat`）
- **过热破绽** `enterOverheat` / `tickOverheat`（字段 `boss.overheatMs` / `damageTakenMul`）

### 三、红 Boss（crimson）

- **连招表 `CLASSIC_COMBOS`**：四档共 13 条（试压 / 扫环 / 封路 / 锁定 / 俯冲战术 / 十字压制 / 终末 / 绞索 / 群压 …），取代扁平随机池。`ATTACK_POOLS` 保留（别处仍引用），但调度走连招。
- **站位锚点 `CLASSIC_ANCHOR`**：弹墙去顶部（才铺得满）、环形弹与十字去中上（才展得开）、激光与俯冲自带位移不预站位。
- **反制走位**：弹墙起手缺口开在玩家常驻列的反面。实测玩家守左侧时缺口平均开在 **77% 处**。
- **过热破绽**：一整套连招后硬直 1300ms、易伤 ×1.6。
- 连招内部衔接 240ms，套与套之间才留 500~1100ms —— 读起来是"一整套"而不是零散单招。

### 四、蓝 Boss（azure）

- **模式序列 `AZURE_SEQUENCES`**：5 条（扫边 `rotor→laneSweep` / 压进 `rain→spinRam` / 切场 `sideColumns→diveCrash` / 巡弋 / 追猎），取代"从普通池或重型池随机挑一个"。
- **过热破绽**：一整套序列后硬直 1300ms、易伤 ×1.6。
- **反制走位**：`laneSweep` 的扫动中心偏向玩家常驻列（±0.18 屏宽，仍是可预判的正弦往返）。实测玩家守左侧时 Boss 平均 x=102 / 屏宽 390。

### 验证

新增 `t_bullets`（弹幕可躲性分析）与 `t_ai2`（红蓝 AI 行为），连同已有的共 **15 个测试台、各跑三遍**全过。

`t_bullets` 的指标值得说明：一开始用"玩家带被弹幕覆盖的百分比"，但**这个指标对整面墙类招式本身就不合适**——墙就该覆盖全屏、只留缝，覆盖率必然很高。改成测**最窄时刻的最宽连续安全走廊**（沿三条常用高度扫描，取最好的一条，再对整招取最小值），才真正对应"能不能无伤过"：

| 招式 | 最窄时刻的走廊宽度 | 机体 24px |
| --- | --- | --- |
| ringSpin | 390px（整屏） | ✓ |
| omniBurst | 130px | ✓ |
| walledBarrier | 57px | ✓ |
| crossSpiral | 163px | ✓ |
| fanSweep | 37px | ✓ |
| laserBeam | 175px | ✓ |

### 已知遗留

- 虚空线 Boss（`updateBossVoid` / `updateBossVoidCore`）没动。它本来就是招式驱动、有大量预警与安全区机制，不属于"老虎机"那类问题；真要调也应该单独一节。
- 过热破绽目前三个 Boss 用同一组参数（1300~1400ms / ×1.6）。如果实战里觉得输出窗口太长或太短，改 `CLASSIC_OVERHEAT_MS` / `AZURE_OVERHEAT_MS` / `HANBA_OVERHEAT_MS` 即可，互不影响。

---

## 2026-09-02 (8) — 旱魃 AI 重写：连招 / 反制走位 / 过热破绽

**动机**：上一节的旱魃是个老虎机——`pickHanbaAttack` 从扁平池里随机抽一招、打完通用漫游、再随机抽一招。招式之间不衔接，也完全不理玩家在干什么，玩家学不到任何东西，只能等。

### 一、连招序列取代随机单招

`HANBA_COMBOS` 按阶段分三组，共 **11 条连招**（其中 10 条是多招连携）。每条有名字，前一招把玩家赶进后一招的杀伤区：

| 阶段 | 连招 | 意图 |
| --- | --- | --- |
| P1 | 驱赶 `sandWall → spikeRow` | 沙墙从上压下把人挤到底部，紧接着底部升矛 |
| P1 | 碾磨 `vortex → sandWall` | 旋沙把人拽向中心，随后环形弹继续压 |
| P1 | 试探 `spikeRow` | 唯一的单招，给玩家喘息 |
| P2 | 围困 `quicksand → duneCharge` | 先标流沙盖住玩家常驻区，再往那里冲撞 |
| P2 | 驱赶改 / 碾磨改 / 沙牢 | 三招收尾或换收招 |
| P3 | 天罚 `callStorm → duneCharge` | 沙暴致盲 + 冲撞，招牌连招 |
| P3 | 绞杀 / 流沙葬 / 驱赶终 | 三段全场压制 |

玩家要学的从"背单招"变成"读连招"。

### 二、站位意图

`COMBO_ANCHOR` 给每招定一个起手位，开打前先移动到位（最多多花 900ms 走位，超时直接起手避免卡住）：沙墙去顶部正中（弹墙才铺得满）、旋沙去屏幕正中（吸引力才有意义）、裂地矛待在上方（避开自己放的矛）。不再是无差别 `defaultMove`。

### 三、反制走位（热度网格）

`boss.hanbaHeat` 是一个 3×3 衰减热度网格（每帧采样玩家所在格，`0.9995^delta` 衰减），用来惩罚龟缩：

- **裂地矛 / 沙墙的缺口开在玩家常驻列的反面** —— 守在左下角，缺口就开右边，逼你横穿全场。
- **流沙第一块直接盖在玩家常驻格上**，另两块随机撒开断退路。

仍然全程有预警，是"逼你动"而不是"不讲道理"。

### 四、过热破绽窗口

一整条连招打完后强制硬直 **1400ms**，期间不出招、**受到伤害 ×1.6**，Boss 周围画一圈脉动光环明确提示"现在可以打"。

这是让战斗有呼吸感的关键：压制 → 破绽 → 压制。也给爆发构筑一个明确的输出窗口，而不是全程无差别输出。

实现上 `bossAi` 写 `boss.damageTakenMul`，[battle.js](src/battle.js) 的子弹与卫星伤害结算各读一次——这是个通用字段，以后别的 Boss 想做易伤窗口直接用。

### 五、阶段变体

同一招在不同阶段打法不同，不用新写招式就让后期更凶：

| 招式 | P1 | P2 | P3 |
| --- | --- | --- | --- |
| 裂地矛 | 7 根 | 9 根 | **11 根 + 第二波错位补刀**（缺口换到另一侧，逼二次移动） |
| 沙墙 | 2 波 / 900ms | 2 波 | **3 波 / 680ms** |
| 冲撞 | 2 段 / 预警 500ms | 3 段 | **4 段 / 预警 380ms** |
| 旋沙 | 吸力 0.055 | 同 | **吸力 0.085 + 叠一圈反向慢环**（交叉出网格状缝隙） |

### 六、怒气打断

单招期间被打掉超过 **8% 最大生命**，立刻中断当前招式、插队一次冲撞反击。爆发构筑不再能无代价站桩输出。

### 验证

新增 `t_ai` 行为测试台，直接驱动旱魃观察它的决策，连同已有的共 **13 个测试台、各跑三遍**全过：

- 48 秒内打出 12 招，序列形如 `vortex → sandWall → spikeRow → sandWall → …`，且当前连招有名字。
- 一整条连招后确实进入过热，易伤 ×1.6，结束后恢复 ×1。
- 玩家死守左下角时，裂地矛缺口平均开在 **72% 处**（右半边）。
- 流沙第一块落在玩家常驻格附近（实测距离 103px，半径 74）。
- 阶段变体：裂地矛 7→11 根、沙墙 2→3 波、冲撞 2→4 段。
- 打掉 12% 血立刻触发怒气打断改冲撞。
- 掉进 P2 立刻强制天罚开场。
- 沙墙起手前确实先移动到屏幕顶部。

**测试上的坑**：反制走位那条一开始不稳定，平均值时好时坏。原因是**旋沙真的会拽动玩家**，"玩家死守左下角"这个前提被 Boss 自己破坏了，热度网格自然测不准。测试里改成每帧把玩家钉回原位（`pinPlayer`）并加大采样量后稳定——这反过来说明旋沙的位移是真生效的。

### 已知遗留

星空的红/蓝 Boss（`updateBossClassic` + `ATTACK_POOLS`）**仍然是随机抽单招**，问题和改前的旱魃一样。这次没动它们，因为改动会影响已有的手感基线。上面这套机制（连招表 / 站位锚点 / 热度网格 / 过热窗口）都是可复用的，想让红蓝 Boss 也升级的话按同样结构套一遍即可。

---

## 2026-09-02 (7) — 沙漠第二批：精英「沙蠕」+ Boss「旱魃」

对应设计案第 3、4 节。至此沙漠的杂兵、精英、Boss 全部换成专属内容，星空的红/蓝 Boss 不再出现在沙漠。

### 先修：30 秒自动开火按**出生时刻**判定

上一节把判据改成排除所有 `fireInterval` 单位，把响尾炮一起排掉了——响尾炮确实算普通敌人，30 秒后应该在自己的弹墙之外补一发单射。同时原判据用的是全局 `state.elapsed`，导致**已经在场上的敌人会在第 30 秒突然一起开火**。

现在：

- 每个敌人在首次被处理的那一帧记下 `spawnElapsed`（懒记录，不用改各处 spawn 调用点）。
- 判据改为 `!enemy.isElite && enemy.type !== "shooter" && !enemy.neverFires`，再加 `enemy.spawnElapsed >= 30000`。
- 效果：30 秒后**新生成的**普通敌人（含响尾炮）会补单射；30 秒前出生的老敌人一辈子不开火；沙蜂与掠沙者靠 `neverFires` 永远不开火。

### 精英「沙蠕」（`sandworm`）

星空精英是"从上方降下来的高血射手"，沙蠕反过来——大部分时间不在场上，只在破土窗口里能被打。节奏从"持续输出"变成"抓窗口爆发"。

| 阶段 | 时长 | 行为 | 可被击中 |
| --- | --- | --- | --- |
| `under` 潜行 | 2000ms（沙暴期 1200ms） | 沙痕朝玩家缓慢移动，只画一道半透明沙痕 | 否 |
| `telegraph` 预警 | 600ms | 位置锁定，地面隆起闪烁 | 否 |
| `erupt` 破土 | 瞬间 | 本体位置放一圈 10 发短程沙柱弹 | — |
| `up` 露出 | 3000ms | 分节蠕虫躯体，每 1.1s 吐 5 路扇形沙弹（±28°） | **是** |

血量沿用星空精英档位 `(50 + lv×0.7) × 1000`，不加厚——难度来自窗口不是血条。掉落走现有精英逻辑，「战利品雷达」照常生效。

### Boss「旱魃」（`bossVariant: "hanba"`）

血量沿用现有 Boss 档位 `(192 + lv/2) × 100000`，按"保持高血高伤"的方针不下调。配色 **`#94a3b8` 石灰白**（风干的骨与石）——与其余三个 Boss 及全部杂兵都不重色，冷灰压在暖沙上最醒目。

独立 driver `updateBossHanba`（因为要控沙暴），招式对象沿用现有 `{ duration, update, move, onStart, onEnd }` 结构，独立成 `HANBA_ATTACKS` / `HANBA_POOLS`。

| 招式 | 时长 | 内容 |
| --- | --- | --- |
| `spikeRow` 裂地矛 | 2200ms | 预警 900ms 后，屏幕下缘升起 7 根沙矛向上冲，随机留 1 个缺口 |
| `sandWall` 横扫沙墙 | 2600ms | 两波 11 发一排从上压下，带 2 个随机缺口 |
| `vortex` 旋沙 | 3000ms | 每帧把玩家往 Boss 中心拽（不夺控制权，可对抗），同时每 700ms 放 16 发旋转环形弹 |
| `quicksand` 流沙陷阱 | 5000ms | 预警 1000ms 后，3 块区域生效，站在里面拖动只走 55% |
| `duneCharge` 尘暴冲撞 | 3400ms | 三段冲刺，每段先画 500ms 路径预警 |
| `callStorm` 沙暴召唤 | 6000ms | 主动拉起沙暴（α 0.7 / 视野 130px），自身半透明，每 500ms 三发瞄准弹 |

**三阶段**（按 HP 比例）：

- **P1 100–60%**：`spikeRow` / `sandWall` / `vortex`，只有可躲的定式招。
- **P2 60–25%**：全部六招。**进入这一档的瞬间强制先放一次 `callStorm`**，作为阶段转换的演出与警告。
- **P3 25–0%**：全部六招 + **常驻弱沙暴**（α 0.35 / 视野 220px）+ 招式间隔 **×0.7**。

### 沙暴与 Boss 的对接

常规沙暴在 Boss 战期间挂起（`sandstormSuspended`），改由旱魃写 `state.bossStorm*`。新增 `applyBossStormOverride()`：两者**取较强的一方**生效，这样 P3 的常驻弱沙暴与 `callStorm` 的浓沙暴能平滑衔接，不会互相打断。

### 场地招式的预警绘制

新增 `drawHanbaTelegraphs(ctx)`，画在背景之上、道具之下（避免盖住弹幕）：流沙区（预警虚线圈 → 生效暗斑）、裂地矛升起位置、尘暴冲撞路径线、旋沙的向内收缩圆环。

### 一个补丁打错位置的 bug

「沙蠕形态」那次编辑的锚点是 `      if (enemy.isElite) {`，本意是 `drawEnemies` 的精英分支，实际匹配到了 **`dropLootFromEnemy` 里选经验球类型的那处**（`drawEnemies` 里是 `} else if`，缩进不同）——绘制代码被塞进了掉落逻辑，导致沙漠里一击杀敌人就 `ReferenceError: ctx is not defined`。

之所以只是**间歇性**失败，是因为要先真的打死一只敌人才会走到那段。已把绘制代码从 `dropLootFromEnemy` 摘回、还原原逻辑，并插到 `drawEnemies` 的正确分支。

教训：锚点只按缩进区分不够，多分支同名条件要连下一行一起锚。

### 验证

新增 `t_b2` / `t_hanba`，连同已有的共 12 个测试台、各跑两遍全过：

- **开火判据**：12s 出生的不开火 / 45s 出生的开火 / 响尾炮算普通敌人 / 沙蜂与掠沙者永不开火。
- **沙蠕**：三阶段都出现、破土触发 3 次、露出期可被击中而潜行与预警期不可、潜行期不开火、露出期每轮固定 5 发扇形。
- **旱魃**：导出六招、P1 三招 / P2 六招、血量档位、配色不与任何已有敌人或 Boss 重复，另三个 Boss 配色不变。
- **整局**：沙漠跑到 120 秒（Boss 于 90s 出场），旱魃招式与场地预警全程无异常；星空回归无异常。
  测试里只开 `godMode` 不开 `oneHitKill`——开秒杀会让 Boss 一枪暴毙直接结算胜利，时钟就停了，反而测不到 Boss 阶段。

### 待做（第三批）

沙漠专属海克斯 8 条 + `entry()` 的 `maps` 参数 + 专属条目的抽取权重加成（初值 2.0）。

---

## 2026-09-02 (6) — 沙漠调整：去暗斑 / 视野改透明度 / 沙蜂高速冲撞 / 资源收紧

第一批的四处试玩反馈修正。

### 沙丘暗斑删除

`drawSandSea` 只保留风成沙纹一层。暗斑本意是"从上方看到的丘背阴影"，实际是几个大黑椭圆盖在场上，糊。

### 视野受限：暗幕挖洞 → 敌人透明度衰减

**改前**：整屏铺沙色暗幕，在玩家周围 `destination-out` 挖一个洞。效果粗糙，而且那个洞本身就很挡视线。

**改后**：

- 暗幕降级成一层**极淡的暖色尘雾**（`alpha = 0.16 × 雾强`，无挖洞），只交代"空气里有沙"，不承担遮挡功能。
- 遮挡改由**敌人透明度**承担：`sandstormEnemyAlpha(enemy)` 按敌人到玩家的距离衰减，近处 `visionR`(150px) 内保持不透明，到 `visionR × 2.6`(390px) 线性降到 `SANDSTORM_MIN_ENEMY_ALPHA = 0.14`，整体再乘沙暴浓度做缓入缓出。
- **敌弹始终全不透明**——看不见的弹幕是不公平，不是难度。
- **Boss 不参与衰减**，否则打 Boss 会变成猜位置。

`drawVisionVeil` 保留，仍供虚空 Boss 使用。

### 沙蜂：高速纯冲撞

| 项 | 改前 | 改后 |
| --- | --- | --- |
| 合速度 | 1.38 | **4.6**（lv10） |
| 入场角 | 0.55~0.90 rad | **0.75~1.10 rad**（横速大于纵速） |
| 开火 | 30 秒后会单发射击 | **完全不开火**（`neverFires`） |

角度偏向横向是关键：合速度很高，但"快"体现在**贴着屏幕高速穿梭 + 边界反弹**上，而不是一秒掉出屏幕。实测穿屏期间反弹 3 次。

顺带修了一个既有 bug：`isNormalEnemy` 原判据是 `!enemy.isElite && enemy.type !== "shooter"`，**响尾炮也被算成普通敌人**，在自己的弹墙之外还会多打一发单射。改为 `!enemy.isElite && !enemy.fireInterval && !enemy.neverFires`，任何自带开火逻辑的单位都排除。

掠沙者一并标记 `neverFires`（同为纯冲撞型）。

### 沙漠资源收紧

新增地图字段 `loot`，不写就是星空的老行为（每只必掉经验球、道具率不打折）：

| 字段 | 沙漠 | 说明 |
| --- | --- | --- |
| `expOrbChance` | 0.5 | 普通敌人掉经验球的概率；**精英与 Boss 不受限，仍必掉** |
| `dropMul` | 0.3 | 道具掉落率倍率，乘在 `dropBase` 上 |

**这会明显拖慢沙漠的升级速度**，到 Boss 时构筑更薄。考虑到沙漠是击败隐藏 Boss 之后才解锁的后期图，方向是对的；但如果实际打起来卡得难受，先调 `expOrbChance` 而不是 `dropMul`——经验直接决定海克斯数量，影响面大得多。

### 测试

新增 `t_d2`（本节四项改动），连同已有的共 10 个测试台全过。

排查沙暴周期用例时踩到三个坑，都不是游戏的问题：

1. 探针原本靠 `createRadialGradient` 判断沙幕是否在画，改成平涂尘雾后失效——改为抓 `fillStyle` 里的 `rgba(120, 82, 28, …)`。
2. 用例原本按"喂了多少帧"推进，但**升级面板会暂停游戏时间**，喂帧数不等于游戏时间。改成盯 HUD 时钟推进（`advanceTo(sc, 秒)`），卡住就全屏扫点消面板。
3. 沙蜂提速后，**测试里不会走位的玩家会在 6 秒内被撞死**，时钟随之停摆。给测试场景开 `debug: { godMode: true }` 把玩家排除出变量——这也算是侧面验证了沙蜂确实变致命了。

---

## 2026-09-02 (5) — 沙漠第一批：沙暴机制 + 五种全新杂兵

对应设计案第 1、2 节。做完这批，沙漠已经是一张玩法与观感都不同的图；精英「沙蠕」与 Boss「旱魃」在第二批。

### 沙暴（[maps.js](src/maps.js) 配置 → [battle.js](src/battle.js) 状态机）

参数整块挂在地图上，**星空不写 `sandstorm` 字段就自然没有沙暴**，代码里没有一处判断地图 id。

| 参数 | 值 | 参数 | 值 |
| --- | --- | --- | --- |
| `calmMs` | 22000 | `enemyMoveMul` | 1.5 |
| `warnMs` | 2000 | `expMul` | 1.5 |
| `activeMs` | 12000 | `dropBonus` | 0.10 |
| `maxAlpha` | 0.66 | `aimSpreadDeg` | 8 |
| `visionR` | 150 | `veilRgb` | 120, 82, 28 |

- 视野受限复用了虚空 Boss 的暗幕挖洞逻辑，抽成通用的 `drawVisionVeil(ctx, rgb, alpha, radius)`，虚空与沙暴各传自己的颜色和半径；洞口用径向渐变做了羽化。
- 敌人加速**只乘本帧位移**（`enemy.x/y` 的 delta），不动敌弹速度——弹幕再加速会变成读条死。
- 沙暴期敌方射击带 ±8° 随机偏移（`stormAimJitter`），射手、精英、响尾炮都吃这个。
- 预警期屏幕四边黄色辉光脉动。
- **Boss 战期间常规循环挂起**（`sandstormSuspended`），沙幕快速退场；主动召唤留给第二批的旱魃。

### 五种全新杂兵（[enemies.js](src/enemies.js)）

刷怪池按地图分流，`pickEnemyType(wave, poolName)` 两套池权重结构对齐，互不混刷。

| 敌人 | id | 对位 | 行为 | 颜色 |
| --- | --- | --- | --- | --- |
| 沙蜂 | `sandmite` | grunt | 3~5 只共用同一斜角成组切入，**撞左右边界会反弹**，在视野里横着犁一阵 | `#84cc16` 酸橙绿 |
| 掠沙者 | `skimmer` | swift | 全游戏唯一**从侧边进场**：`speed=0` + `vx≠0`，贴屏幕下半横掠 | `#e11d48` 玫红 |
| 沙丘龟 | `dunecrawler` | tank | 高血慢速，**死亡爆散 8 发环形弹**（`deathBurst` → `spawnDeathBurst`） | `#1e3a8a` 海军蓝 |
| 响尾炮 | `rattler` | shooter | 停在 y=100，发射 **9 发带 2 个随机缺口的横向弹墙**缓慢下压 | `#0e7490` 深青 |
| 掘地者 | `burrower` | weaver | **潜沙 1.2s（`untargetable`，不可击中也不碰撞）↔ 冒头 0.8s**，冒头瞬间朝玩家冲刺；沙暴期潜行缩短 40% | `#f5f5f4` 骨白 |

**配色约束**：不与任何既有敌人重复（旧色为 `#ef4444` / `#f59e0b` / `#7c3aed` / `#10b981` / `#c026d3` / `#facc15` / `#dc2626` / `#0ea5e9` / `#a855f7`）。沙漠这套一律走冷色与骨白，与暖色沙地形成冷暖对比——旧敌人全是暖色，放在沙地上会糊。

`spawnEnemyForWave` 现在可能返回**数组**（沙蜂编队），`spawnRegular` 相应处理。

### 战斗核心的三处改动

1. `untargetable` 豁免——子弹命中与撞机判定各加一处跳过，掘地者潜沙期子弹会穿过去。这是之前没有的概念。
2. `spawnDeathBurst(enemy)` 挂在 `onEnemyDefeated` 开头，尸爆弹用默认敌弹配色（`#f472b6`），暖色在沙地上会糊。
3. 敌人出界回收**原本就检查了 x 轴**（`e.x > -80 && e.x < W + 80`），设计案里"要补 x 轴判定"那条提醒不成立，掠沙者直接可用。

### 沙漠观感：俯视沙海

第一版做成了天空渐变 + 地平线 + 沙丘剪影，**方向错了**——游戏是俯视角，侧视的地平线与剪影和视角割裂，而且剪影几乎看不出在动。改为：

- **平涂沙面** + 两层向下漂移的地面质感（`drawSandSea`），做出"机体在沙海上飞行"的感觉：
  - 沙丘暗斑：从正上方看到的丘背阴影，大而慢（5 个椭圆，alpha 0.16）
  - 风成沙纹：一道道平行波纹，细而稍快（间距 26px，alpha 0.5）——是质感不是花纹
- 风沙颗粒保留并分三档粗细与速度，做出"风里的尘"的层次。
- 色带改为中低明度暖沙，保证玩家子弹（`#facc15`）与冷色敌人始终读得出来：

| 波次 | 沙面 | 沙纹 | 颗粒 |
| --- | --- | --- | --- |
| 暮色沙丘 | `#4a3117` | `#5c3d1d` | `#e8bc70` |
| 正午黄沙 | `#6b4d22` | `#7d5b2a` | `#f7dda0` |
| 赤色沙暴 | `#5a2510` | `#6d2f15` | `#f0894a` |
| 夜漠 | `#221c14` | `#2b241a` | `#b9a689` |

星空的平涂背景与星点完全不受影响。

### 验证

新增三个测试台，连同已有的共 9 个全过：

- **刷怪分池**：沙漠池刷出全部 5 种且不混入星空敌人，反之亦然。
- **配色**：五种新敌人均不与 9 个旧敌人色值重复，彼此也不重色。
- **沙蜂**：编队共用同一入场角度；穿屏期间反弹 2 次、从不越界、反弹不损失横向速度；下落 1.38，纵向穿屏约 10.2 秒。
- **掠沙者**：`speed=0 / vx≠0`、从左右外侧进场、出现在屏幕下半。
- **掘地者**：潜行与冒头都会出现，潜行期 `untargetable=true`、冒头期 `false`。
- **响尾炮**：每轮固定 7 发（9 发扣 2 个缺口）。
- **沙暴周期**：t=10s 无沙幕 → t=30s 沙幕生效 → t=42s 退场 → t=62s 第二轮按周期到来；星空 t=32s 始终无沙幕。（测试里要顺手点掉升级面板，否则 `pausedForUpgrade` 会卡住时间推进。）
- **烟测**：沙漠跑 64 秒 update+draw 无异常，星空回归无异常。

### 待做（第二批）

精英「沙蠕」、Boss「旱魃」，以及旱魃的 `callStorm` 招式与 `sandstormSuspended` 的对接。沙漠目前的 Boss 仍是星空的红/蓝 Boss。

---

## 2026-09-02 (4) — 可重复词条 + 池抽空自动补给 + 控制台兑换码

### 可重复词条机制（[src/upgrades.js](src/upgrades.js)）

`entry()` 新增第 7 个参数 `options`：

- `repeatable: true` — 可反复刷到、反复选。**不写入 `picked`，也不吃 `blocked`**（否则一次「剔除」或永雏塔菲的自动剔除就把"反复刷到"废掉了）。
- `availableIf(s)` — 额外可用条件，返回 false 时不进候选池。

| 词条 | 改动 |
| --- | --- |
| **高级重掷**（`reroll_premium`） | 改为可重复；`availableIf = hasPurplePlusAvailable`，**池中没有紫/橙可抽时自动从候选池消失** |
| **应急修复**（`heal_quick`） | 改为可重复，无额外条件 |

`hasAnyUpgradeAvailable()` 语义同步改成**只统计不可重复的词条**——可重复词条永远在池里，拿它们判断"池已抽空"会导致面板永远弹下去。

配套在 [battle.js](src/battle.js) 三处剔除逻辑里加了 `o.repeatable` 豁免：`blockCurrentUpgradePanelAsUnpicked`、`blockTaffyUnpickedSiblings`、`pruneUpgrade`。

### 池抽空 → 自动补给，不再弹窗

改在 `openUpgradePanel()` 开头短路，两个调用点（`gainExp` 升级、`consumeItem` 的 LV+ 道具）一次覆盖：

```
不可重复词条全部拿完
  → 不设 pausedForUpgrade，不弹面板
  → grantAutoLevelReward()：回满生命（溢出照常走「超量血库」）+ 100 金币
  → HUD 左上角显示 1.8 秒提示「补给空投  生命全满  +100 金币」
```

`gainExp` 的 `while` 循环因为不再 `break`（没有暂停），一帧内连升多级会**逐级发放**补给，符合"之后每次升级自动回满血或者获得金币"。

原来的两选一补给面板 `buildPostPoolFallbackOptions()` 保留为安全网，正常流程已到不了。

### 开发者控制台兑换码

| 项 | 值 |
| --- | --- |
| 兑换码 | `912989446` |
| 效果 | 主菜单右上角常驻 DEV 入口 |
| 存档字段 | `devConsoleUnlocked`（默认 `false`，清档会重置） |

原来 [menu.js](src/menu.js) 顶部的 `const DEV_ENABLED = false` 改成 `devEnabled()` 函数读存档，四处调用点同步。想强制开调试就把函数体改成 `return true`。

### 验证

- 可重复词条：连选 5 次仍可选、5 次治疗都结算；紫/橙抽空后高级重掷消失而应急修复仍在；`hasAnyUpgradeAvailable` 只剩可重复时返回 false；`pickUpgrades` 仍能抽到应急修复。
- 自动补给：拿光不可重复词条后进入自动模式。
- 兑换码：模拟 `wx.showModal` 回填 `912989446`，走完整设置面板 → 兑换 → 解锁 → 重进主菜单 DEV 入口常驻。
- 全量回归 6 个测试台全过；满构筑数值未受影响（对 Boss DPS 仍 7,551,934）。

### 已知副作用

两条可重复词条会**常驻候选池**，池子越到后期越小，它们的相对出率越高——后期面板经常会是「应急修复 + 高级重掷 + 一条真词条」。这是"反复刷到"的直接代价；若觉得挤，可给可重复词条单独调低抽取权重。

---

## 2026-09-02 (3) — 新增地图系统（星空 / 沙漠）

**动机**：主菜单加入地图选择，默认战场命名为「星空」，并把「沙漠」作为击败隐藏 Boss 的奖励内容。非数值改动，但影响存档结构与菜单交互，记在这里方便回溯。

### 新文件 [src/maps.js](src/maps.js)

地图定义表，结构与 `characters.js` / `talents.js` 同级：

| 字段 | 含义 |
| --- | --- |
| `themes` | 背景色带，按波次循环取用 `(waveIndex - 1) % themes.length`，与 `config.THEMES` 同结构 |
| `particle` | 远景颗粒风格：`"star"` 纵向下坠星点（2×2）/ `"sand"` 横向吹拂沙粒（3×1） |
| `unlockBy` | `null` 默认可用；`"hiddenBoss"` 需击败虚空线 Boss，**解锁前不在菜单展示**（沿用兑换码角色「未解锁不露出」的约定） |
| `accent` | 菜单卡片选中态描边色 |

| 地图 | 色带 | 颗粒 | 解锁 |
| --- | --- | --- | --- |
| **星空**（`starfield`） | 深蓝 `#020617` → 紫 `#0b0420` → 绿 `#031918` → 红 `#1a0606`（沿用原 `config.THEMES`） | star | 默认 |
| **沙漠**（`desert`） | 暮色沙丘 `#1c1204` → 正午黄沙 `#2a1a06` → 赤色沙暴 `#2b1005` → 夜漠 `#0f0b06` | sand | 击败隐藏 Boss |

`config.THEMES` **保留不动**，作为 `maps.js` 取不到色带时的兜底。

### 存档结构（[src/storage.js](src/storage.js)）

新增两个字段，`load()` 里按 `unlockedCharacters` / `talents` 同样的方式做默认值合并，**旧存档无需迁移**：

- `unlockedMaps: { starfield: true, desert: false }`
- `selectedMapId: "starfield"`

新增 `unlockMap(id)`（首次解锁返回 `true`，用于结算提示去重）与 `setSelectedMapId(id)`。

### 战斗接入（[src/battle.js](src/battle.js)）

- 开局用 `resolveSelectedMap(save)` 取地图；**存档里记的地图若未解锁会自动回落星空**，不会卡住。
- `applyTheme()` 的色带来源由 `THEMES` 改为地图的 `mapThemes`。
- `drawBackground()` 按 `state.mapParticle` 分支绘制星点 / 沙粒。
- 新增 `resolveVoidWin(enemy)`：虚空线 Boss 通关时解锁「沙漠」并返回是否询问接续无尽。三处胜利分支（子弹击杀 / 卫星击杀 / 反伤击杀）原先各自写 `isVoidLineBoss(enemy)`，统一改为调用它，避免以后再漏一处。
- 胜利界面在金币行下方多一行 `已解锁新地图：沙漠`（仅本局首次解锁时显示）。

### 菜单交互（[src/menu.js](src/menu.js)）

**布局位置一律没动。**「天赋」与「地图」共用原天赋列表那块可视区（`getPanelViewport()` 即原 `getTalentViewport()`），横向滑动切换：

- 拖动超过 **屏宽 20%** 翻页，否则回弹；翻页动画 180ms easeOutCubic，跟手拖拽。
- 只有两页，**左右滑都切到对面那页**——「右滑天赋栏进入地图」和反向滑动都成立。
- 按下时先锁方向：横向位移 ≥12px 且大于纵向 → 翻页；纵向 ≥8px → 滚动当前页。一次拖拽内不改判，避免斜滑时页面和列表一起动。
- 标题随页切换「天赋 / 地图」，右侧加两个页码圆点；**「重置天赋」按钮只在天赋页绘制与响应**。
- 地图卡：左侧色带缩略图（用该地图第一档主题色实时画）+ 名称 + 描述 + 「使用中 / 选择」徽标；点击即切换并落盘。
- 还有未解锁地图时，列表底部显示 `？？？ 击败隐藏 Boss 可解锁新战场`。

### 验证

无微信环境，用 stub 掉 `wx` 的 Node 测试台跑通（脚本见「怎么复测」）：

- 菜单 20 项断言全过：默认停天赋页、右滑进地图页、小幅拖动回弹、反向滑回、纵向拖动不翻页、未解锁时沙漠不出现、解锁后出现、点击切换并落盘、开始游戏带上地图。
- 战斗侧：星空取 `#020617`、沙漠取 `#1c1204`、存了未解锁地图时回落星空、`unlockMap` 首次 `true` 重复 `false`。
- 烟测：两张地图各跑 100 秒 `update + draw`（跨第二波 60s / 第三波 90s 的换主题），菜单跑 200 帧，均无异常。

### 已知遗留

- **地图目前只有视觉差异**（背景色带 + 颗粒风格），没有玩法差异。刷怪表 / 敌人属性 / 波次时长都还是共用的。要做差异化，在 `maps.js` 加字段、`battle.js` 读 `battleMap` 即可，接入点已经留好。
- 沙漠没有专属 BGM，仍走 `audio_bgm_main` / `audio_bgm_boss`。

---

## 2026-09-02 (2) — 暴击顺序依赖修复 + LV+ 升级包改为词条解锁

**动机**：清掉上一节留下的「暴击溢出顺序相关」；另外 LV+（直升一级）不该默认送给玩家。Boss 血量与伤害**明确保持不变**（见下）。

### 满构筑实测对照

破袭者、无局外天赋、拿光全部可用词条、全弹命中：

| 指标 | 原始（本日改动前） | 上一节之后 | 本节之后 |
| --- | --- | --- | --- |
| 对 Boss DPS | 17,033,046 | 9,209,346 | **7,551,934（累计 −56%）** |
| 爆伤倍率 | 7.700（顺序相关，4.80~7.70 漂） | 5.865（同样漂） | **4.800（恒定）** |
| Boss（1920 万血）TTK | 1.13 秒 | 2.08 秒 | **2.54 秒** |
| 虚空核心（4960 万血）TTK | 2.91 秒 | 5.39 秒 | **6.57 秒** |
| 可抽到的词条数 | 58 / 61 | 61 / 61 | **62 / 62** |

### 机制改动：暴击溢出改为最后统一结算

**改前** — [upgrades.js](src/upgrades.js) `addCritRateWithOverflow` 把溢出直接加进 `critMult`，之后的 `critMult *= N`（瞄准训练III/IV）会把这部分一起放大。同一组词条换个拾取顺序，满构筑爆伤在 **4.80 ~ 7.70** 之间漂。

**改后** — 拆成两步：

- `addCritRateWithOverflow` 只累计 `s.critOverflowRate`，不再碰 `critMult`。
- 新增导出 `effectiveCritMult(s)` = `critMult + critOverflowRate × 1.5`（比率常量 `CRIT_OVERFLOW_TO_MULT`），伤害结算处（[battle.js:2432](src/battle.js:2432)）改为调用它。
- `state.critOverflowRate` 默认值加在 [battle.js:400](src/battle.js:400)。

**验证**：对 6 张暴击相关词条（瞄准训练 I–IV、终端优化、完美机师）跑完 `6! = 720` 种拾取顺序，结果收敛到唯一一组 `暴击 0.95 / 爆伤 4.800`。顺带把顺序上限 5.865 拉到恒定 4.800，本节 DPS 因此再降 18%。

### LV+ 升级包：从默认掉落改为词条解锁

**改前** — Boss 战周期掉落表里 `levelup` 占 22%，无任何前置，等于白送多次免费三选一。

**改后**：

| 位置 | 改动 |
| --- | --- |
| [battle.js:2071](src/battle.js:2071) Boss 周期掉落 | 移除 `levelup`。概率重分配（总和 100）：无敌 22→**28** / 治疗 18→**23** / 磁吸 14→**18** / 炸弹 14→**18** / 金币 10→**13** |
| [battle.js:2088](src/battle.js:2088) 新增空投逻辑 | 仅 `state.levelUpSupply` 为真时生效，**35~55 秒**随机间隔从屏幕上方空投一枚 LV+ |
| [upgrades.js](src/upgrades.js) 新增词条 | **空投信标**（`supply_beacon`，purple）「本局对战的剩余时间内，战场会不定期空投升级包（LV+）」→ `s.levelUpSupply = true` |

LV+ 现在只有两条来源，都要先付代价：**空投信标**（定期空投），或**战术学习III**（`exp_quantum`，敌人阵亡 5% 概率掉落）。

同步更新：[items.js](src/items.js) 里 `levelup` 的注释（原写「仅 Boss 战随机掉落」已不成立）、[README.md](README.md) 道具小节。

### 设计决定：Boss 血量与伤害保持不变

> **⚠️ 本节这条决定已被 2026-09-02 (11) 撤销**——Boss 血量已按「正常玩家 5 分钟打完」重定标。下面保留原文仅作记录。

上一节「已知遗留」提到玩家 DPS 削弱后可考虑同步下调 Boss 血量。**决定不下调**——保持 Boss 高血量高伤害、让胜利更难取得，是刻意的成就感设计。

因此：

- [enemies.js:178](src/enemies.js:178) Boss `(192 + lv/2) × 100000`、[enemies.js:220](src/enemies.js:220) 虚空核心 `(496 + lv/2) × 100000` —— **不动**。
- Boss 弹 450 / 撞机 6000、精英撞机 4000、普通撞机 2000 —— **不动**。
- 后续若还要削玩家，默认同样**不要**跟着削 Boss；除非前期 Boss 战被拖到明显无聊，再单独评估。

### 已知遗留（未改）

- 上一节的「Boss 血量与成长曲线不匹配」现已转为上述**刻意设计**，不再视为待办。
- 满构筑射击间隔仍被「神格降临」的 `shootIntervalFloor = 30` 钉住，射速线的调整对满构筑无影响，只影响前中期。若想让射速线在后期也有区分度，得改的是这个下限而不是词条百分比。

---

## 2026-09-02 — 海克斯强度回调 + 护盾/反伤机制修正

**动机**：玩家能刷到的词条组合过强，满构筑下 Boss 1 秒左右融掉，且拿到任意一张护盾牌后基本免疫弹幕。

### 满构筑实测对照

破袭者、无局外天赋、把全部可用词条拿光、假设全弹命中：

| 指标 | 改前 | 改后 | 变化 |
| --- | --- | --- | --- |
| 对 Boss DPS | 17,033,046 | 9,209,346 | **−46%** |
| Boss（1920 万血）TTK | 1.13 秒 | 2.08 秒 | +84% |
| 虚空核心（4960 万血）TTK | 2.91 秒 | 5.39 秒 | +85% |
| 护盾每秒回复量 | 1125 | 970 | −14% |
| 护盾回复可抵挡敌弹 | 1.88 发/秒 | 1.62 发/秒 | −14% |
| 最大生命 | 7500 | 12937 | +72%（见「装甲板线复活」） |
| 可抽到的词条数 | 58 / 61 | **61 / 61** | 修掉 3 张永远刷不到的牌 |

> 护盾"每秒回复百分比"砍半后绝对值只降 14%，是因为同一批改动复活了装甲板线、把 maxHp 顶上去了（护盾上限 = maxHp）。若不拿装甲板线，护盾回复就是干净的 −50%。

### 护盾类

| 词条 | 改前 | 改后 | 原因 |
| --- | --- | --- | --- |
| 能量护盾（`shield_basic`） | 每秒修复 3% | **1.5%** | 指定下调 50% |
| 能量护盾II（`shield_extra`） | 每秒修复 6% | **3%** | 同上 |
| 能量护盾III（`shield_rapid`） | 每秒修复 15% | **7.5%** | 同上 |

三条另外把 `s.shieldMaxHp = s.maxHp` 改成 `Math.max(s.shieldMaxHp || 0, s.maxHp)`。原来是直接赋值，被「超量血库」撑大护盾之后再拿一张护盾牌，会把上限**砍回** maxHp。

### 伤害天花板

| 词条 | 改前 | 改后 | 原因 |
| --- | --- | --- | --- |
| 高能弹芯IV（`dmg_xl`） | 伤害 +100% | **+75%** | 单卡乘区过大 |
| 瞄准训练IV（`crit_apex`） | 暴伤 +100% | **+70%** | 与瞄准训练III 的 ×1.5 连乘后爆炸 |
| 暴击溢出转爆伤（`addCritRateWithOverflow`） | 每 1% 溢出 → +2% 爆伤 | **+1.5%** | 暴击率累计 160%、上限 95%，溢出 65% 白送 +130% 爆伤 |
| 虚空收割者（`bullet_void`） | 弹道 +6（`sideBullets += 6`） | **+4，且走 `Math.min(8, ...)`** | 原写法绕过分裂弹幕线的 8 上限，弹道能到 21 发；现在封顶 17 发 |
| 绝对领域（`shield_absolute`） | 反伤倍率 +5 | **+3** | 反伤按帧结算，倍率过高时贴脸即秒杀 |
| 战术学习II（`exp_smart`） | green，经验 +200% | **blue，+150%** | 与同为 green 的 `exp_basic`（+100%）严格重叠且更强；经验是全局最强复利来源 |

### 射速线（重做为"拿满 = 初速 ×3"）

| 词条 | 改前 | 改后 |
| --- | --- | --- |
| 急速射击（`fr_basic`） | 射速 +32% | **+25%** |
| 急速射击II（`fr_cool`） | +64% | **+50%** |
| 急速射击III（`fr_turbo`） | 描述 +128% / 代码 ×1.28 | **+60%** |

`1.25 × 1.5 × 1.6 = 3.0`，即设计意图"三条拿满等于初始射速的 3 倍"。实测（`shootInterval` 整数取整）：

| 角色 | 初始 | 拿满三条 | 实际倍率 |
| --- | --- | --- | --- |
| 破袭者 | 420ms | 140ms | 3.000 |
| 重炮手 | 500ms | 166ms | 3.012 |
| 侦察兵 | 380ms | 126ms | 3.016 |
| 守望者 | 460ms | 153ms | 3.007 |
| 机械师 | 440ms | 146ms | 3.014 |
| 吸血鬼 / 永雏塔菲 | 400ms | 133ms | 3.008 |
| 棱镜 | 430ms | 143ms | 3.007 |

子弹尺寸惩罚**未动**（仍是 `0.9 / 0.81 / 0.729`，累计 0.531），但原描述三条都写"缩小10%"与实现不符，已改成真实数字：`缩小10% / 再缩小19% / 再缩小27%`。
（若本意是每条各缩 10%（累计 0.729），改代码把后两条设为 `0.9` 即可——那是一次**加强**，故未擅自做。）

### Bug 修复：装甲板线复活

`hp_heavy` 的前置写成了它自己，`hp_plate` 又依赖 `hp_heavy` → 整条线（装甲板 I/II/III）永远无法出现在三选一里，但仍显示在图鉴中。

| 词条 | 改前 | 改后 |
| --- | --- | --- |
| 装甲板（`hp_plate`） | 前置 `hp_heavy`，最大HP +20% | **无前置，+15%** |
| 装甲板II（`hp_heavy`） | 前置 `hp_heavy`，+30% | **前置 `hp_plate`，+20%** |
| 装甲板III（`hp_unbroken`） | 前置 `hp_heavy`，+40% | 前置不变，**+25%** |

数值一并下调的原因：按原值 `1.2 × 1.3 × 1.4 = 2.18` 复活后，满构筑 maxHp 会从 7500 冲到 16380，而护盾上限跟随 maxHp，等于把这次护盾削弱整个抵消掉。压到 `1.15 × 1.2 × 1.25 = 1.725` 后为 12937。

### 机制改动（`src/battle.js`）

1. **护盾溢出穿透** — [battle.js:2613](src/battle.js:2613)（敌弹）、[battle.js:2708](src/battle.js:2708)（撞机）
   改前：护盾 `> 0` 就整发吃掉伤害并 `continue`，剩 1 点护盾也能完全免疫一发 450 的 Boss 弹；护盾回复逐帧结算，几乎不可能连续两帧为 0 → **只要 `shieldRegenPerSec > 0` 就近似免疫弹幕**，百分比调多少都不太影响体感。
   改后：残余护盾按承伤倍率折算成"能挡下的伤害量"，吃不下的部分穿透到生命值。护盾回复的百分比从此真正决定强度。

2. **反伤不再能白嫖** — [battle.js:2657](src/battle.js:2657)
   改前：反伤结算排在无敌帧判定**之前**，撞机又没有碰撞冷却 → 吃一个 INV 道具贴着 Boss，每帧 `撞击伤害 × 反伤倍率` 零代价输出。
   改后：条件加上 `state.invincibleMs <= 0`。无敌**道具**期间不结算反伤；DEV `godMode` 行为保持不变（仍可反伤，便于调试 `devOneHitKill`）。撞机冷却未加——加了会降低受伤频率，属于加强，与本次方向相反。

3. **超量血库护盾封顶** — [battle.js:1383](src/battle.js:1383)
   改前：`maxHp` 有 `baseMaxHp × 11` 的天花板，`shieldMaxHp` 没有 → 过量治疗可以把护盾无限撑大。
   改后：`shieldMaxHp` 沿用同一个 `ceiling`，`shieldHp` 也不再能超过 `shieldMaxHp`。

### 已知遗留（未改，供后续判断）

- **暴击溢出转爆伤是顺序相关的**（[upgrades.js:104](src/upgrades.js:104)）：`addCritRateWithOverflow` 先把溢出加到 `critMult`，之后的 `critMult *= N` 会把这部分一起放大。同一组词条不同拾取顺序，满构筑爆伤可差 4.8 ↔ 5.87。要消除就得把溢出改成最后统一结算。
- **Boss 血量与玩家成长曲线不匹配**：Boss `(192 + lv/2) × 100000`，90 秒开打时玩家往常只有 10~15 个词条，靠 Boss 战期间的小怪继续升级滚雪球。若后续还要再削玩家，得同步下调 [enemies.js:178](src/enemies.js:178) 的 Boss 血量，否则前期 Boss 会拖得很久。

### 怎么复测

**纯数值**：`src/upgrades.js` 是纯 CommonJS、不依赖 `wx`，直接 Node `require` 即可——构造假 state，反复扫描 `UPGRADE_POOL` 把 `available()` 的词条全拿光，打印满构筑属性与对 Boss DPS，再和 `git show HEAD:src/upgrades.js` 的版本做同样一遍对照。这个扫描同时能查出前置链断裂（自依赖 / 悬空 prereq 导致永远刷不到的词条）。

**菜单 / 战斗**：这两个模块依赖 `wx`，在 Node 里先塞一个最小 stub 即可跑：`getWindowInfo`、`getStorageSync` / `setStorageSync`（用内存对象）、`createImage`、`createInnerAudioContext`、`loadSubpackage`、`onTouchStart` 等空函数，再补一个全局 `requestAnimationFrame`。`draw(ctx)` 传一个 Proxy 假 ctx：拦 `fillText` 收集文本用来断言页面内容，拦 `fillStyle` 的 setter 读背景色。触摸直接调 `scene.onTouchStart / onTouchMove / onTouchEnd` 喂坐标即可模拟滑动与点击。

基线常量：敌弹 300 伤害、Boss 弹 450、撞机 普通 2000 / 精英 4000 / Boss 6000、护盾承伤 ×2、`bossDmgMul` 初始 0.65、`eliteDmgMul` 初始 0.8、射速下限 80ms（神格降临可降到 30）、maxHp 上限 `baseMaxHp × 11`、弹道数 = `2 × sideBullets + 1`（sideBullets 上限 8）。

